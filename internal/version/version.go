// Package version holds build metadata for dlexa.
package version

// Build metadata constants injected at compile time.
const (
	BinaryName = "dlexa"
	Version    = "0.1.0-dev"
	Commit     = "dev"
	BuildDate  = "unknown"
)
