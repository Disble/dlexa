# Skill Registry

Generated by `sdd-init` for project `dlexa`.
Last updated: 2026-04-06 (re-init for multi-module cobra architecture, Strict TDD enabled)

## Available User Skills

| Skill | When to load | Source |
| --- | --- | --- |
| `go-testing` | Writing Go tests, table-driven tests, golden tests, httptest integration tests | `~/.config/opencode/skills/go-testing/SKILL.md` |
| `kin` | Any task that uses external library APIs, version-pinned libraries, or library-internal errors | `~/.config/opencode/skills/kin/SKILL.md` |
| `kin-init` | Setting up KIN integration or project docs routing | `~/.config/opencode/skills/kin-init/SKILL.md` |
| `skill-creator` | Creating new AI skills or agent instruction bundles | `~/.config/opencode/skills/skill-creator/SKILL.md` |
| `sdd-explore` | Investigating a feature or exploring architecture before committing to a change | `~/.config/opencode/skills/sdd-explore/SKILL.md` |
| `sdd-propose` | Creating or updating a change proposal | `~/.config/opencode/skills/sdd-propose/SKILL.md` |
| `sdd-spec` | Writing or updating specifications for a change | `~/.config/opencode/skills/sdd-spec/SKILL.md` |
| `sdd-design` | Creating or updating a technical design document | `~/.config/opencode/skills/sdd-design/SKILL.md` |
| `sdd-tasks` | Breaking a change into an implementation task checklist | `~/.config/opencode/skills/sdd-tasks/SKILL.md` |
| `sdd-apply` | Implementing tasks from a change (Strict TDD required) | `~/.config/opencode/skills/sdd-apply/SKILL.md` |
| `sdd-verify` | Validating that implementation matches specs and tasks | `~/.config/opencode/skills/sdd-verify/SKILL.md` |
| `sdd-archive` | Syncing delta specs and archiving a completed change | `~/.config/opencode/skills/sdd-archive/SKILL.md` |
| `cognitive-complexity` | Measuring Cognitive Complexity, auditing understandability, refactoring to reduce complexity | `~/.config/opencode/skills/cognitive-complexity/SKILL.md` |

## Project Instruction Surfaces

- `AGENTS.md` — active repo-local agent routing, delegation guardrails, workflow rules
- `README.md` — canonical public project definition
- `CONTRIBUTING.md` — lint pre-commit onboarding, repo workflow
- `skills/` — canonical project-local skills (`.claude/skills/` are mirrors; keep in lockstep)

## Project-Local Skills

| Skill | When to load | Source |
| --- | --- | --- |
| `dlexa-go-cli-lint` | Validating Go/CLI changes with the repo-pinned lint workflow, without building | `skills/dlexa-go-cli-lint/SKILL.md` |
| `dlexa-skill-updater` | Updating `dlexa-user` docs when CLI, semantic DPD behavior, project positioning, discovery guidance, or mirror parity drifts | `skills/dlexa-skill-updater/SKILL.md` |
| `dlexa-sonarqube-mcp` | Running the repository's SonarQube MCP workflow in the required order | `skills/dlexa-sonarqube-mcp/SKILL.md` |
| `dlexa-user` | Using `dlexa` for DPD-style normative doubts and distinguishing those cases from out-of-scope generic dictionary tasks | `skills/dlexa-user/SKILL.md` |
| `no-duplication` | Eliminating Go test duplication detected by SonarQube; managing testdata fixture CPD exclusions | `.claude/skills/no-duplication/SKILL.md` |

## Project Positioning Notes

- `dlexa` is DPD-first: a consultation interface for normative doubts in Spanish, not a generic dictionary replacement.
- Valid use cases include orthographic, orthoepic/pronunciation, morphological, syntactic, and lexico-semantic doubts covered by the DPD.
- Guidance must preserve contextual nuance: current usage, cultured formal norm, register, geography, and communicative context can matter.
- The CLI is migrating from a manual `flag`-based parser to `spf13/cobra` multi-command surface (Search, DPD, DLE, etc.).

## Detected Architectural Conventions

- Language: Go 1.22 (`go.mod`)
- Entry point is thin: `cmd/dlexa/main.go`
- Composition root lives in `internal/app/wiring.go`
- Core use case is query orchestration in `internal/query`
- Source adapters follow explicit `fetch → parse → normalize` pipeline in `internal/source`
- Shared domain language lives in `internal/model`
- Rendering stays outside the domain in `internal/render`
- Cache is explicit cache-aside in `internal/cache`
- Platform/OS boundary is isolated in `internal/platform`
- Doctor/version/config are isolated support boundaries
- **Target**: cobra command tree replacing manual `flag` parsing in `internal/app/app.go`

## Detected Tooling

- Test runner: `go test ./...` — unit + httptest integration, covers app/cache/fetch/normalize/parse/query/render/source
- Coverage: `go test -cover ./...`
- Linter: `go tool --modfile=golangci-lint.mod golangci-lint run ./...` (full); `--new-from-rev=HEAD` (pre-commit)
- Type checker: `go vet ./...`
- Formatter: `gofmt`
- Pre-commit: `.pre-commit-config.yaml` (golangci-lint diff-based hook)
- SonarQube: `sonar-project.properties` present
- CI: none detected in `.github/workflows/`

## Active Change

- `dpd-live-lookup-parity` — phase: `apply`, 3 tasks pending (3.5, 5.7, 5.8)
- Archived changes: `dpd-terminal-semantic-rendering`, `performance-improvement`, `dpd-signs-preservation`
- Engram-archived: `add-dpd-entry-search-command`, `adopt-search-first-consultation-flow`

## TDD Status

**Strict TDD Mode: ENABLED** ✅
All `sdd-apply` runs MUST follow RED → GREEN → REFACTOR for every task.

## Sub-Agent Loading Guidance

1. Try Engram first for `skill-registry` (topic_key: `skill-registry`).
2. Fall back to this file when Engram is unavailable.
3. Read `AGENTS.md` before acting on repo-local workflow or skill-routing assumptions.
4. Load `dlexa-user` before using the CLI for a DPD-style consultation task.
5. Load `dlexa-skill-updater` before maintaining `dlexa-user` or its mirrors.
6. Load `go-testing` before writing Go tests.
7. Load `kin` before using any external library API or version-sensitive dependency.
8. Load `dlexa-go-cli-lint` after every code change for lint validation.
9. Load `no-duplication` when SonarQube flags CPD issues or before writing repetitive test boilerplate.
10. Follow README project-definition and architecture notes before proposing structural changes.
